/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Adios",
  assetPrefix: "/Adios/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
